/**
 * TS mirrors of the CSS design tokens (src/index.css `@theme`) for the few
 * consumers that cannot read CSS custom properties: Leaflet vector layers
 * (SVG presentation attributes) and canvas-like drawing. Keep in sync with
 * the `@theme` block.
 */
export const ACCENT = '#4ADE80';
export const ON_ACCENT = '#0B2916';
